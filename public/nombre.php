<?php
$entier = 42;
$decimal = 2.5;

// Addition
echo "$entier + $decimal = ". ($entier + $decimal) ."<br>";

// Soustraction
echo "$entier - $decimal = ". ($entier - $decimal) ."<br>";

// Multiplication
echo "$entier * $decimal = ". ($entier * $decimal) ."<br>";

// Division
echo "$entier / $decimal = ". ($entier / $decimal) ."<br>";

// Modulo
echo "$entier % $decimal = ". ($entier % $decimal) ."<br>";

// Post Incrémentation
echo ( $entier++ )."<br>";

// Pre Incrémentation
echo ( ++$entier )."<br>";

